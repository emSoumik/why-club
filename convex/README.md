# Convex backend

This directory contains the Convex backend for WhÿClub.

The `_generated` files in this workspace are intentionally lightweight stubs so
the project can compile before a real Convex deployment is linked. After you
configure a deployment with `npx convex dev`, rerun Convex code generation and
replace these stubs with the official generated files.
